using UnityEngine;

public interface IPlayerModule : IBrainModule
{
    // This interface is now empty - all methods come from IBrainModule
    // Kept for backward compatibility with existing player modules
}
